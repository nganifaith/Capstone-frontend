import styled from 'styled-components';

const RatingWrapper = styled.div`
  display: flex;

  i {
    color: #ccc;
    font-size: 18px;
  }

  i.Active {
    color: #ffa013;
  }
`;

export default RatingWrapper;
